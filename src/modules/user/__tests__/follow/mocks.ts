import { Entities } from '../../../../common/types';

import { MOCKED_USERS } from '../../__mocks__/user';

export const userChangeFollowingStateMocks: Entities = {
  users: MOCKED_USERS,
  parties: [],
};
